import { PageHeader } from "@/components/page-header";
import { getAnalytics } from "@/lib/api/server";
export default async function AnalyticsPage() { const data = await getAnalytics(); const metrics = [["Total missions", data.total_missions], ["Completion rate", `${data.completion_rate_pct}%`], ["Verification rate", `${data.verification_rate_pct}%`], ["Average completion", data.average_completion_seconds == null ? "Unavailable" : `${Math.round(data.average_completion_seconds)}s`]]; return <main className="mx-auto max-w-6xl px-4 py-6 sm:px-6 lg:px-8"><PageHeader eyebrow="Measured outcomes" title="Analytics" description="Mission completion, verification, duration, status, and provider results." /><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{metrics.map(([label, value]) => <div key={label} className="rounded-xl border bg-card p-5"><div className="font-mono text-2xl font-semibold">{value}</div><div className="mt-1 text-sm text-muted-foreground">{label}</div></div>)}</div><div className="mt-5 grid gap-5 lg:grid-cols-2"><section className="rounded-xl border bg-card p-5"><h2 className="font-semibold">Missions by status</h2><div className="mt-4 space-y-3">{data.missions_by_status.map((item) => <div key={item.status} className="flex justify-between gap-3 border-b pb-3 text-sm"><span className="capitalize">{item.status.replaceAll("_", " ")}</span><span className="font-mono">{item.count}</span></div>)}</div></section><section className="rounded-xl border bg-card p-5"><h2 className="font-semibold">Executions by provider</h2><div className="mt-4 space-y-3">{data.executions_by_provider.map((item) => <div key={item.provider} className="grid grid-cols-3 gap-3 border-b pb-3 text-sm"><span className="capitalize">{item.provider}</span><span>{item.verified} verified</span><span>{item.failed} failed</span></div>)}</div></section></div></main>; }
